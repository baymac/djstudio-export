[project]
name = "dj"
dynamic = ["version"]
requires-python = ">=3.11,<3.13"
dependencies = [
    "pyrekordbox",
    # detect
    "rich>=13",
    "instagrapi>=2.1",
    "shazamio>=0.4",
    "httpx>=0.27",
    "curl_cffi>=0.7",
    "python-dotenv>=1.0",
    "yt-dlp>=2025.5",
    "mutagen>=1.47",
    "cryptography>=42",
    "psutil>=5.9",
    "browser-cookie3>=0.20",
    # sync
    "playwright>=1.58",
    "playwright-stealth>=1.0",
]

[project.scripts]
dj = "dj_cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = {file = "VERSION"}

[tool.setuptools]
py-modules = ["dj_cli", "paths", "caffeinate", "assets", "update", "doctor"]

[tool.setuptools.packages.find]
include = ["detect*", "sync*", "enrich*", "playlist*", "export*", "connections*", "djstudio*", "rekordbox*", "helpers*", "apps*", "vj*"]

[tool.setuptools.package-data]
# Read-only source files accessed by external tools (swiftc, node).
"connections.bridge" = ["*.swift"]
"enrich" = ["dj_studio_sdk.js"]
# Extension source (read-only, no npm at runtime).
"apps" = ["**/*.json", "**/*.js", "**/*.ts", "**/*.css", "**/*.html", "**/*.png", "**/*.svg"]
# Pre-built frontend assets (built by CI before wheel build).
"apps.course" = ["dist/**/*"]
"vj.cats" = ["dist/**/*"]
# VJ package.json for list_apps() discovery.
"vj" = ["**/package.json"]
